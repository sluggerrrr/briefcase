# Railway Environment Variables Template for Briefcase Backend
# Copy this file to Railway Dashboard > Variables section
# Replace all placeholder values with your actual configuration

# Database Configuration (Railway provides DATABASE_URL automatically when you add PostgreSQL)
DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}

# Security Keys (Generate strong random strings for production)
SECRET_KEY=your-secret-key-here-use-openssl-rand-hex-32
MASTER_KEY=your-master-encryption-key-here-use-openssl-rand-hex-32
ENCRYPTION_KEY=${MASTER_KEY}  # Reuse master key for document encryption

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Configuration
# Add your frontend domain(s) here, comma-separated
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://yourdomain.railway.app

# Application Settings
ENVIRONMENT=production
LOG_LEVEL=info
DEBUG=false

# Rate Limiting (optional)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_PERIOD=60  # seconds

# Document Storage Settings
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_TYPES=pdf,doc,docx,txt,png,jpg,jpeg

# Lifecycle Management Settings
CLEANUP_GRACE_PERIOD_DAYS=30
NOTIFICATION_DAYS_BEFORE_EXPIRY=7,1
AUDIT_LOG_RETENTION_DAYS=2555  # 7 years
CLEANUP_BATCH_SIZE=100
ENABLE_EXPIRATION_NOTIFICATIONS=true

# Email Settings (optional, for notifications)
EMAIL_ENABLED=false
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@yourdomain.com

# Railway automatically provides these:
# PORT - The port your app should listen on
# RAILWAY_ENVIRONMENT - The Railway environment (production/staging)
# RAILWAY_PUBLIC_DOMAIN - Your app's public URL